# StudyMate 서비스 기획서

## 1. 서비스 개요

| 항목 | 내용 |
|---|---|
| 서비스명 | StudyMate |
| 서비스 한 줄 소개 | 공부할 분량과 기간을 입력하면 AI가 날짜별 Todo-list를 만들어주는 학습 계획 서비스 |
| 서비스 목적 | 막연한 공부 목표를 오늘 바로 실천할 수 있는 날짜별 계획으로 바꾼다. |
| 핵심 가치 | `슬라이드 500장`처럼 입력한 전체 분량을 날짜에 맞춰 나누고, 각 날짜에 시작·끝 범위를 제시한다. |

## 2. 문제와 해결 방법

학습자는 시험이나 과제 전까지 공부해야 할 양은 알고 있어도, 이를 매일 어느 정도씩 해야 하는지 정하기 어렵다. 계획이 구체적이지 않으면 미루기 쉽고, 남은 기간에 비해 분량이 과도한지도 판단하기 어렵다.

StudyMate는 학습자 정보, 공부 주제, 전체 분량, 시작일, 목표일, 하루 공부 가능 시간을 입력받는다. AI는 입력된 기간의 모든 날짜를 포함하고, 날짜마다 바로 실행할 수 있는 Todo 3개를 만든다. 숫자로 표현된 분량은 가능한 한 고르게 나누어 `슬라이드 1~167장 읽기`처럼 범위를 명시한다.

## 3. 타겟 사용자

| 사용자 | 필요한 도움 |
|---|---|
| 중학생 | 시험 범위와 과제를 며칠에 나누어 공부할지 정하는 계획 |
| 고등학생 | 내신·수능 과목의 많은 분량을 목표일까지 배분하는 계획 |
| 대학생 | 전공 교재, 강의 슬라이드, 발표 준비 자료를 계획적으로 학습하려는 학습자 |
| 성인 학습자 | 자격증, 외국어, 직무 학습을 꾸준히 진행하는 계획 |

## 4. 페이지·섹션 구성과 이동 방식

StudyMate는 한 화면에서 사용할 수 있는 단일 페이지 서비스이며, 상단 메뉴의 앵커 링크를 사용해 각 섹션으로 이동한다.

| 섹션 | 역할 |
|---|---|
| 홈 | 서비스 소개와 `공부 계획 만들기` 버튼 제공 |
| 이용 방법 | 입력부터 결과 확인까지의 사용 순서 안내 |
| 공부 계획 | 학습 정보를 입력하고 AI 계획을 요청하는 핵심 기능 |
| FAQ | 분량 입력 방식과 계획 생성에 대한 자주 묻는 질문 안내 |

모바일에서는 입력칸을 세로로 배치하고 버튼을 넓게 표시해 작은 화면에서도 쉽게 입력할 수 있도록 구성한다.

## 5. 핵심 기능

### 5-1. 학습 정보 입력

사용자는 다음 항목을 입력한다.

- 학습자 유형: 중학생, 고등학생, 대학생, 성인
- 과목 또는 학습 주제: 예) 심리학
- 공부할 분량: 예) 슬라이드 500장, 교재 1~8장, 수학 문제 200개
- 시작일과 시험일 또는 목표일
- 하루 공부 가능 시간: 1시간부터 5시간 이상까지 선택

### 5-2. AI 날짜별 Todo-list 생성

`공부 계획 만들기` 버튼을 누르면 입력값을 확인한 뒤 AI에 계획 생성을 요청한다. 결과에는 시작일부터 목표일까지의 모든 날짜가 포함되며, 각 날짜에는 정확히 3개의 Todo가 표시된다.

예시 입력:

```text
대학생 / 심리학 / 슬라이드 500장 / 2026-09-01 ~ 2026-09-03 / 하루 2시간
```

예시 출력:

```text
9월 1일
□ 슬라이드 1~167장 읽기
□ 핵심 개념 3개 정리
□ 학습 내용 복습
```

## 6. AI 기능 설계

### 입력

프론트엔드는 다음 JSON 데이터를 `/api/recommend`로 보낸다.

```json
{
  "learnerType": "대학생",
  "subject": "심리학",
  "amount": "슬라이드 500장",
  "startDate": "2026-09-01",
  "goalDate": "2026-09-03",
  "studyTime": "2시간"
}
```

### 처리

1. JavaScript가 빈값, 날짜 순서, 최대 계획 기간 60일을 확인한다.
2. Vercel Python Serverless Function이 같은 값을 다시 검증한다.
3. Python 함수가 환경 변수 `GEMINI_API_KEY`를 사용해 Gemini API에 계획 생성을 요청한다.
4. AI 요청에는 다음 기준을 포함한다.
   - 모든 날짜를 빠짐없이 포함한다.
   - 날짜별 Todo는 정확히 3개로 작성한다.
   - 숫자 분량은 날짜 수로 나누고 시작·끝 범위를 표시한다.
   - 범위 없는 추상적 문장 대신 읽기, 정리, 복습처럼 행동이 보이는 문장을 작성한다.
5. Python 함수는 AI 결과가 날짜별 계획 JSON 구조와 일치하는지 다시 확인한다.

### 출력

백엔드는 아래 형태의 JSON만 프론트엔드에 반환한다.

```json
{
  "dailyPlans": [
    {
      "date": "2026-09-01",
      "tasks": ["슬라이드 1~167장 읽기", "핵심 개념 정리", "학습 내용 복습"]
    }
  ]
}
```

JavaScript는 이 데이터를 날짜별 Todo 카드로 바꾸어 화면에 표시한다.

## 7. 사용자 상태와 실패 처리 기준

| 상황 | 처리 방식 | 사용자에게 보이는 안내 |
|---|---|---|
| 필수 입력 누락 | 요청 전 JavaScript에서 중단 | `필수 정보를 입력해주세요.` |
| 목표일이 시작일보다 빠름 | 요청 전 JavaScript와 Python에서 중단 | `목표일은 시작일 이후로 선택해주세요.` |
| 계획 기간 60일 초과 | 요청 전 JavaScript와 Python에서 중단 | `계획 기간은 최대 60일까지 선택해주세요.` |
| AI 요청 진행 중 | 버튼을 비활성화하고 결과 영역 전환 | `AI가 공부 계획을 만들고 있습니다...` |
| Gemini API 오류 또는 네트워크 오류 | 결과 영역에 오류 상태 표시 | 잠시 후 다시 시도하라는 안내 |
| 응답 지연 | 20초 뒤 요청을 중단 | `AI 응답이 오래 걸리고 있습니다. 잠시 후 다시 시도해주세요.` |
| AI 결과 형식 오류 | Python과 JavaScript에서 결과를 거부 | AI 응답 형식 오류 안내 |

## 8. 기술 구성

| 구분 | 사용 기술 | 역할 |
|---|---|---|
| 프론트엔드 구조 | HTML | 섹션, 메뉴, 입력 폼, 결과 영역 구성 |
| 디자인 | CSS | 색상, 여백, 카드 디자인, 모바일 반응형 적용 |
| 화면 동작 | Vanilla JavaScript | 입력 검증, `fetch` 요청, 로딩·성공·실패 화면 처리 |
| 백엔드 | Python Vercel Serverless Function | 요청 검증, Gemini API 호출, JSON 응답 반환 |
| AI | Gemini API | 날짜별 구체적인 학습 Todo 생성 |
| 배포 | GitHub + Vercel | 코드 관리와 공개 URL 배포 |

## 9. 보안·운영 원칙

- Gemini API 키는 코드에 작성하지 않고 Vercel 환경 변수 `GEMINI_API_KEY`로만 관리한다.
- 로컬 키 파일 `.env`는 `.gitignore`에 포함되어 GitHub에 올라가지 않는다.
- 브라우저 JavaScript에는 API 키를 넣지 않는다. 브라우저는 `/api/recommend`만 호출한다.
- 과목은 100자, 공부 분량은 150자, 계획 기간은 최대 60일로 제한해 과도한 요청을 줄인다.
- API 키가 노출된 경우 해당 키를 즉시 폐기하고 새 키를 발급한 뒤 Vercel 환경 변수를 교체한다.

## 10. 기대 효과와 확인 방법

학습자는 전체 분량을 날짜별로 나눈 구체적인 계획을 즉시 받아, “무엇을 공부할지”를 고민하는 시간을 줄일 수 있다. 서비스 동작은 다음 세 가지 입력으로 확인한다.

| 테스트 | 입력 또는 행동 | 기대 결과 |
|---|---|---|
| 정상 계획 | `슬라이드 500장`, 3일 | 날짜마다 슬라이드 시작·끝 범위가 포함된 Todo 표시 |
| 빈 입력 | 아무 값 없이 버튼 클릭 | 필수 입력 안내 표시 |
| 날짜 오류 | 목표일을 시작일보다 앞서 선택 | 날짜 오류 안내 표시 |

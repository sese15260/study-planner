# StudyMate 서비스 기획서

## 1. 서비스 목적

StudyMate는 학습자가 공부할 분량과 기간을 입력하면, 매일 실천할 구체적인 Todo-list를 만들어주는 AI 학습 계획 서비스입니다. 막연한 계획 대신 `슬라이드 1~167장 학습`처럼 바로 실행할 수 있는 범위를 제안하는 것이 핵심 가치입니다.

## 2. 타겟 사용자

- 중학생: 시험 범위와 과제 계획이 필요한 학습자
- 고등학생: 내신·수능 범위를 날짜별로 나누고 싶은 학습자
- 대학생: 전공 교재, 강의 슬라이드, 발표 자료를 계획적으로 학습하려는 학습자
- 성인 학습자: 자격증, 외국어, 직무 학습을 꾸준히 진행하려는 학습자

## 3. 페이지와 섹션 구성

| 섹션 | 역할 |
|---|---|
| 홈 | 서비스의 목적과 공부 계획 만들기 버튼 제공 |
| 이용 방법 | 입력부터 계획 확인까지의 사용 순서 안내 |
| 공부 계획 | 학습 정보 입력, AI 계획 요청, 결과 Todo-list 표시 |
| FAQ | 분량 입력 방식과 계획 생성 방식 안내 |

상단 메뉴는 각 섹션의 `id`로 이동하는 앵커 링크 방식입니다.

## 4. 핵심 AI 기능

### 입력

- 학습자 유형
- 과목 또는 학습 주제
- 공부할 분량
- 시작일과 목표일
- 하루 공부 가능 시간

### 처리

프론트엔드 JavaScript가 `/api/recommend`로 JSON을 보냅니다. Vercel Python 함수는 입력값을 검증한 뒤 OpenAI API에 날짜별 계획 생성을 요청합니다. 숫자 분량이 있는 경우 시작·끝 범위를 명시하도록 프롬프트에 포함합니다.

### 출력

시작일부터 목표일까지 모든 날짜를 포함하며, 날짜마다 정확히 3개의 구체적인 Todo를 JSON으로 반환합니다. JavaScript는 이를 Todo 카드로 변환해 화면에 표시합니다.

### 실패 처리 기준

| 상황 | 사용자 안내 |
|---|---|
| 필수값 누락 | `필수 정보를 입력해주세요.` |
| 날짜 오류 | `목표일은 시작일 이후로 선택해주세요.` |
| 60일 초과 | `계획 기간은 최대 60일까지 선택해주세요.` |
| OpenAI API 오류 | 잠시 후 다시 시도하라는 안내 |
| 응답 지연 | 20초 뒤 시간 초과 안내 |
| API 키 설정 누락 | AI 기능 설정 문제 안내 |

## 5. 보안과 운영 원칙

- API 키는 Vercel의 `OPENAI_API_KEY` 환경 변수에서만 관리한다.
- `.gitignore`로 `.env`와 `.vercel` 폴더를 GitHub에서 제외한다.
- 프론트엔드에는 API 키를 포함하지 않는다.
- 계획 요청은 최대 60일, 과목 100자, 분량 150자로 제한해 과도한 호출을 줄인다.

# StudyMate 평가 기준 답변서

## 항목 1

### 1-1. 배포 URL로 접속 가능하고, 최소 3개 이상의 페이지/섹션이 메뉴 이동으로 확인되는가?

**답변:** 네. 배포 URL은 [https://studymate-navy.vercel.app/](https://studymate-navy.vercel.app/)입니다. 상단 메뉴에서 홈, 이용 방법, 공부 계획, FAQ의 4개 섹션으로 이동할 수 있습니다. 단일 페이지이지만 각 메뉴가 앵커 링크로 해당 섹션으로 이동하므로 최소 3개 이상의 섹션과 메뉴 이동 요구사항을 충족합니다.

### 1-2. 모바일에서 레이아웃이 깨지지 않고 정상 동작하는가?

**답변:** 네. `css/style.css`의 media query를 사용해 모바일에서는 입력 항목을 세로로 배치하고, 버튼을 화면 너비에 맞게 넓히며, 메뉴와 글자 크기·여백을 조정했습니다. 모바일에서도 메뉴, 입력 폼, 날짜별 Todo 카드가 화면 안에 표시됩니다.

### 1-3. AI 기능이 입력 → 요청 → 결과 표시까지 정상 동작하는가?

**답변:** 네. 사용자가 학습 정보를 입력하면 JavaScript가 JSON 요청을 보내고, Python Serverless Function이 Gemini API를 호출합니다. Gemini가 만든 날짜별 계획 JSON을 받으면 JavaScript가 날짜별 Todo 카드로 화면에 표시합니다. 숫자 분량은 날짜별 시작·끝 범위가 보이도록 요청합니다.

### 1-4. 정상/빈값/긴 입력 등 2~3개 테스트 입력으로 동작이 재현 가능한가?

**답변:** 네. 정상 입력은 `대학생 / 심리학 / 슬라이드 500장 / 10일 / 하루 2시간`으로 테스트할 수 있고, 날짜별 범위 Todo가 표시됩니다. 빈값으로 제출하면 필수 입력 안내가 표시됩니다. 과목 100자 또는 분량 150자를 넘는 긴 입력은 Python 서버에서 길이 검증 오류로 처리합니다.

### 1-5. 실패 상황에서 사용자 안내 메시지가 1개 이상 표시되는가?

**답변:** 네. 빈 입력에는 `필수 정보를 입력해주세요.`, 날짜 오류에는 `목표일은 시작일 이후로 선택해주세요.`를 표시합니다. 요청 중에는 로딩 문구를 표시하고, 90초 이상 응답이 없으면 시간 초과 안내를 표시합니다. API 키·모델 설정 오류는 결과 영역의 오류 안내로 표시합니다.

### 1-6. API 키가 코드에 노출되지 않았고, 환경 변수로만 관리되는가?

**답변:** 네. Gemini API 키는 Vercel 환경 변수 `GEMINI_API_KEY`에서만 읽습니다. 키는 `api/recommend.py`, `js/app.js`, README, 이미지, GitHub 커밋에 작성하지 않았습니다. 로컬 `.env`와 `.env.local`은 `.gitignore`에 포함되어 있습니다.

### 1-7. 제출 패키지 필수 5종이 모두 제공되는가?

**답변:** 제공됩니다. 배포 웹서비스는 Vercel URL로, 코드는 GitHub 저장소로 제공합니다. `README.md`에는 서비스 소개·기술 스택·실행 및 배포 방법·환경 변수 설명이 있고, `SERVICE_PLAN.md`에는 서비스 기획서가 있습니다. `images/` 폴더에는 데스크톱, 모바일, AI 실행 결과, 오류 처리, Codex 과정, Vercel 배포 증빙 이미지가 있습니다.

## 항목 2

### 2-1. HTML/CSS/JS/API 구조를 왜 이렇게 나눴는가?

**답변:** 파일마다 역할을 분리해 수정 범위를 작게 만들기 위해서입니다. `index.html`은 메뉴·폼·결과 영역의 구조를 담당합니다. `css/style.css`는 색상·여백·카드·모바일 반응형을 담당합니다. `js/app.js`는 입력 검증, 버튼 이벤트, `fetch`, 결과 렌더링을 담당합니다. `api/recommend.py`는 API 키를 숨긴 상태로 서버 검증과 Gemini 호출을 담당합니다. 그래서 디자인 수정은 CSS, 화면 동작 수정은 JS, AI 요청 수정은 Python에서 독립적으로 할 수 있습니다.

### 2-2. AI 기능의 프론트 로직에서 로딩/성공/실패 상태를 어떻게 처리했는가?

**답변:** 제출 버튼을 누르면 `showLoading()`으로 로딩 영역을 보이고 버튼을 비활성화합니다. 성공하면 `renderPlan()`이 `dailyPlans` 데이터를 날짜별 Todo 카드로 만듭니다. 빈 입력이나 날짜 오류는 `showMessage()`로 입력 폼 가까이에 표시합니다. API·네트워크·시간 초과 오류는 `showResultError()`로 결과 영역에 표시합니다. `AbortController`로 90초가 지나면 요청을 중단합니다.

### 2-3. Serverless Function(Python)에서 입력 검증과 응답 포맷을 어떻게 정했는가?

**답변:** Python의 `validate_plan_input()`에서 필수값, 학습자 유형, 공부 시간, 날짜 형식, 날짜 순서, 최대 60일, 과목 100자, 분량 150자를 다시 확인합니다. 브라우저 검증만 믿지 않고 서버에서도 검증해 잘못된 요청을 막습니다. 응답은 화면이 바로 사용할 수 있도록 `dailyPlans` 배열 안에 `date`와 Todo 3개가 들어가는 JSON 형식으로 정했습니다. `validate_ai_plan()`이 모든 날짜와 Todo 3개 구조를 한 번 더 확인합니다.

### 2-4. 배포 후 발생한 문제를 어떤 순서로 진단하고 수정했는가?

**답변:** 먼저 배포 URL에서 문제를 재현했습니다. 그다음 Vercel Logs에서 `/api/recommend`의 상태 코드와 Gemini 오류를 확인했습니다. 모델 404 오류는 API 키가 사용할 수 있는 모델로 변경했습니다. Gemini의 일시적 502 오류는 서버에서 한 번 재시도하고, 계속 실패하면 날짜별 대체 계획을 반환하도록 보완했습니다. 수정 후 GitHub에 push해 Vercel이 재배포하도록 하고, 배포 URL에서 다시 계획 생성 결과를 확인했습니다.

## 항목 3

### 3-1. HTML/CSS/JavaScript의 역할 차이를 서비스 코드 예시로 설명할 수 있는가?

**답변:** HTML은 `index.html`의 `<form>`, `<label>`, `<section>`처럼 화면 구조와 접근성을 담당합니다. CSS는 `style.css`에서 카드 모양, 색상, 여백, 모바일 media query를 담당합니다. JavaScript는 `app.js`에서 `handlePlanSubmit()`으로 입력을 받고, `renderPlan()`으로 응답 데이터를 화면에 넣는 동작을 담당합니다.

### 3-2. fetch 요청이 서버리스 함수로 전달되고 응답이 돌아오는 흐름을 설명할 수 있는가?

**답변:** `handlePlanSubmit()`이 입력값을 JSON으로 만든 뒤 `fetch()`로 `POST /api/recommend` 요청을 보냅니다. Vercel은 `api/recommend.py`를 `/api/recommend` Serverless Function으로 실행합니다. Python이 Gemini API를 호출해 JSON 응답을 반환하면, JavaScript가 `response.json()`으로 읽고 `renderPlan()`에 전달해 Todo 카드로 표시합니다. Live Server에서는 Python Function을 직접 실행할 수 없기 때문에 배포된 StudyMate API를 호출하도록 분기했습니다.

### 3-3. 환경 변수를 쓰는 이유를 보안/운영 관점에서 설명할 수 있는가?

**답변:** 보안상 API 키를 브라우저 코드나 GitHub에 넣으면 방문자가 키를 복사해 임의 호출과 비용 발생을 일으킬 수 있습니다. 운영상 환경 변수를 사용하면 개발·미리보기·배포 환경별로 다른 키를 설정할 수 있고, 키를 교체할 때 소스 코드를 수정하거나 재커밋하지 않아도 됩니다. 키가 유출되면 기존 키를 폐기하고 Vercel 환경 변수만 새 키로 바꾸면 됩니다.

### 3-4. AI 기능을 이 서비스에 넣은 이유와 프롬프트를 어떻게 구성했는가?

**답변:** 사용자는 총 공부 분량과 목표일은 알아도 매일 해야 할 일을 나누기 어렵기 때문에 AI 기능을 넣었습니다. 프롬프트에는 한국어 사용, 시작일부터 목표일까지 모든 날짜 포함, 하루 Todo 정확히 3개, 숫자 분량의 시작·끝 범위 표시, 읽기·정리·복습처럼 구체적인 행동 작성, JSON 스키마만 반환이라는 조건을 넣었습니다. 그래서 추상적인 계획 대신 바로 실행할 수 있는 Todo-list를 만들도록 했습니다.

## 항목 4

### 4-1. 응답 지연이 잦다면 어떤 개선 옵션을 고려할 수 있는가?

**답변:** 더 빠르고 비용이 낮은 모델을 사용하고, 입력 길이·최대 60일·하루 Todo 3개 제한으로 요청과 응답 크기를 줄일 수 있습니다. 같은 입력에는 이전 결과를 재사용하는 캐싱을 추가할 수 있습니다. 요청 버튼 중복 클릭을 막고, 일시 오류는 짧게 재시도합니다. 현재 서비스에는 90초 시간 제한, 한 번 재시도, 대체 계획이 적용되어 있습니다.

### 4-2. AI 기능을 2개로 늘린다면 프론트/백엔드 구조를 어떻게 확장하는가?

**답변:** 예를 들어 두 번째 기능으로 학습 계획 점검을 추가할 수 있습니다. 프론트엔드에는 점검 입력 폼, 점검 버튼, 점검 결과 영역을 별도로 만들고, 백엔드에는 `api/review.py`를 추가해 `/api/review` 엔드포인트로 분리합니다. 공통 입력 검증과 로딩·오류 처리 함수는 재사용하되, 각 AI 기능의 프롬프트와 JSON 응답 형식은 분리합니다.

### 4-3. API 키가 유출됐다면 즉시 취해야 할 조치와 재발 방지 방법은 무엇인가?

**답변:** 먼저 Google AI Studio에서 기존 키를 즉시 폐기합니다. 새 키를 발급해 Vercel의 `GEMINI_API_KEY`를 교체하고 새 배포에서 동작을 확인합니다. 그다음 GitHub 커밋, README, 이미지, 프론트 코드에 키가 남아 있는지 확인합니다. 재발 방지를 위해 `.gitignore`에 `.env`를 유지하고, 키를 화면에 캡처하거나 코드에 작성하지 않으며, 커밋 전에 비밀값 포함 여부를 확인합니다.

### 4-4. 프론트 프레임워크가 허용된다면 어떤 장단점과 변경 범위를 예상하는가?

**답변:** React나 Vue를 쓰면 입력값, 로딩, 성공, 실패 상태를 컴포넌트 단위로 관리하기 쉽고 AI 기능이 여러 개로 늘어날 때 화면 재사용이 편합니다. 반면 패키지 설치와 빌드 과정이 추가되고, 현재 HTML 구조를 컴포넌트로 나누며 JavaScript DOM 조작 코드를 상태 기반 코드로 다시 작성해야 합니다. 현재 StudyMate는 단일 페이지와 하나의 핵심 폼이므로 바닐라 HTML/CSS/JavaScript가 더 단순하고 과제 조건에도 맞습니다.

## 증빙 이미지 확인

| 이미지 파일 | 증명하는 내용 |
|---|---|
| `desktop-home.png` | 배포 화면과 상단 메뉴, 섹션 구성 |
| `mobile-layout.jpg` | 모바일 반응형 화면 |
| `ai-plan-result-500-slides.png` | AI 실행 결과와 날짜별 Todo-list |
| `loading-state.png` | 로딩 상태 처리 |
| `validation-error1.png` | 목표일 날짜 오류 안내 |
| `validation-error2.png` | 필수 입력 누락 안내 |
| `codex-process.png` | Codex 사용 과정 |
| `vercel-deployment.png` | Vercel 배포 완료 상태 |
